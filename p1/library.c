#include <stdio.h>

int main() {


    int num, par, impar;

    for (num >= 100; num <= 200; num++);
    if (num % 2 == 0) {
        printf("Es par");
    } else {
        printf("Es impar");
    }

    return 0;
}